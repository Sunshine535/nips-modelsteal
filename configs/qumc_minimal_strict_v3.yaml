# R3 strict v3: add CE-only and topK+CE controls to isolate Q-UMC mechanism
# Per GPT55 R3 Task 6: must beat ce_only and ce_plus_strict_topk to claim Q-UMC works
model_name: "Qwen/Qwen2.5-0.5B"
num_steps: 5000
batch_size: 8
seq_len: 128
lr: 0.00002
kd_temp: 2.0
kd_alpha: 0.7
topk: 20
probe_tokens: 2000
heldout_probe_tokens: 500
cal_examples: 200
carlini_examples: 100
eval_every: 500
eval_batches: 50
perturb_std: 0.01
max_probe_budget: -1
basis_source: "carlini_recovered"
variants:
  - ce_only
  - strict_topk_kd
  - strict_topk_weighted_tailzero
  - completion_no_unc_strict
  - completion_uncertainty_strict
  - completion_uncertainty_normed_kl
  - full_logit_upper
